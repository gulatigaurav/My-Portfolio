<div class="card" data-id="<%= item.id %>">
  <%= image_tag item.thumb_image, width: '100%' %>
  <p class="card-text">
     <span><%= link_to item.title, portfolio_display_path(item) %> </span>
     <%= item.subtitle %>
  </p>
</div>
