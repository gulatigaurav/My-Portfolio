<p> <%= image_tag @item.main_image if @item.main_image%></p>
<h1> <%=  @item.title %> </h1>
<h3><%=  @item.subtitle %></h3>
<p> <%=  @item.body %> </p>

<h2> Technologies used </h2>
<% @item.technologies.each do |t| %>
   <p> <%= t.name %> </p>
<% end %>
<%= link_to "Back", portfolios_path %>
